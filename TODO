- revisit exception handling when socket closes or half-closes
- make Connection a generic
- check if TCP_NODELAY is active
- mass_inform
- strict timestamp parsing
- mechanism to enforce human readable ASCII on certain strings
- timestamp_or_now type
- Sphinx and type hints
- Test coverage
- required requests (restart, logging)
- fill in TODOs in the documentation, and make them actually appear somewhere
- tidy up the rendering of the documentation: there are some issues e.g. the Informs
  sections of the request docstrings aren't valid numpydoc.
- check if mock_transport is going to be useful
