[build-system]
requires = ["setuptools", "setuptools_scm"]

[tool.setuptools_scm]
version_file = "src/aiokatcp/_version.py"

[tool.isort]
profile = "black"
line_length = 100

[tool.black]
line-length = 100
